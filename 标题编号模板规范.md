# 标题编号解构式规范 v2

## 1. 目标

本规范定义一套可高度自定义、可结构化配置、可清晰映射到 UI 的标题编号体系。

核心目标：

- 不再以单个模板字符串作为唯一中心。
- 将标题编号拆解为可复用的部件。
- 让 UI 可以按“第一列看最终预览，第二列选外壳，第三列选内部样式”的方式直接操作。
- 同时支持多级编号，如 `1.1`、`1.1.1`、`（一）`、`第一章`。
- 支持“当前级显示样式”和“被下级引用时的样式”不同。

## 2. 为什么不能只用单模板

单模板模式看起来简单，但会遇到一个本质问题：

- 一级标题想显示成 `第一章`
- 二级标题又想显示成 `1.1`

如果一级标题只有一套模板与一套样式，那么下级引用一级时到底应该拿：

- `第一章`
- `一`
- `1`

这三个不是一回事。

所以必须把下面几层语义拆开：

- 当前级自己显示时的“外壳”
- 当前级自己显示时的“内部数值样式”
- 当前级被下级引用时的“引用样式”
- 多级编号的“组合链”

## 3. 总体模型

编号系统拆成 4 张表 + 1 张绑定表：

1. `shell_catalog`
   作用：定义外壳，例如 `第{}章`、`（{}）`、`{}、`、`{}`。
2. `core_style_catalog`
   作用：定义数值本体样式，例如 `1`、`01`、`一`、`A`。
3. `chain_catalog`
   作用：定义多级编号如何组合，例如 `仅当前`、`一级.当前`、`一级.二级.当前`。
4. `preset_catalog`
   作用：把常见组合预设成快捷选项，例如 `第一章`、`1.1`、`（一）`。
5. `level_bindings`
   作用：每一级标题实际绑定哪个外壳、哪个内部样式、哪个引用样式、哪个组合链。

这才是解构式模型。

## 4. 部件一：外壳表 `shell_catalog`

### 4.1 定义

外壳只负责包裹一个“值槽”，不负责决定这个值用什么数字风格。

例如：

- `第{}章`
- `第{}节`
- `{}`
- `{}、`
- `（{}）`
- `({})`
- `附录{}`

### 4.2 建议数据结构

```json
{
  "shell_catalog": {
    "plain": {
      "label": "{}",
      "prefix": "",
      "suffix": ""
    },
    "chapter_cn": {
      "label": "第{}章",
      "prefix": "第",
      "suffix": "章"
    },
    "section_cn": {
      "label": "第{}节",
      "prefix": "第",
      "suffix": "节"
    },
    "dunhao_cn": {
      "label": "{}、",
      "prefix": "",
      "suffix": "、"
    },
    "paren_cn": {
      "label": "（{}）",
      "prefix": "（",
      "suffix": "）"
    },
    "paren_en": {
      "label": "({})",
      "prefix": "(",
      "suffix": ")"
    },
    "appendix_cn": {
      "label": "附录{}",
      "prefix": "附录",
      "suffix": ""
    }
  }
}
```

### 4.3 关键约束

- 外壳必须且只允许一个值槽。
- 值槽统一记作逻辑占位，不建议直接暴露任意字符串模板。
- 自定义外壳仍然允许，但也必须满足“一个值槽”的约束。

## 5. 部件二：内部数值样式表 `core_style_catalog`

### 5.1 定义

内部数值样式只定义整数 `1, 2, 3...` 如何显示。

它不包含任何括号、顿号、章、节、附录等文本。

### 5.2 内置样式建议

| id | UI 标签 | 示例 |
| --- | --- | --- |
| `arabic` | `1 2 3` | `1` |
| `arabic_pad2` | `01 02 03` | `01` |
| `cn_lower` | `一 二 三` | `一` |
| `cn_upper` | `壹 贰 叁` | `壹` |
| `roman_upper` | `I II III` | `I` |
| `circled` | `① ② ③` | `①` |
| `circled_paren` | `⑴ ⑵ ⑶` | `⑴` |

### 5.3 边界说明

- `（一）` 不是内部数值样式，而是：
  - 外壳 = `（{}）`
  - 内部数值样式 = `一 二 三`
- `第一章` 不是内部数值样式，而是：
  - 外壳 = `第{}章`
  - 内部数值样式 = `一 二 三`

## 6. 部件三：多级组合链表 `chain_catalog`

### 6.1 定义

多级组合链只定义“哪些层级值会被取出并按什么顺序连接”，不直接定义外壳。

例如：

- `仅当前`
- `一级.当前`
- `一级.二级.当前`
- `一级-当前`

### 6.2 建议数据结构

```json
{
  "chain_catalog": {
    "current_only": {
      "label": "仅当前",
      "segments": [
        {"type": "value", "source": "current"}
      ]
    },
    "l1_dot_current": {
      "label": "一级.当前",
      "segments": [
        {"type": "value", "source": "level1"},
        {"type": "literal", "text": "."},
        {"type": "value", "source": "current"}
      ]
    },
    "l1_dot_l2_dot_current": {
      "label": "一级.二级.当前",
      "segments": [
        {"type": "value", "source": "level1"},
        {"type": "literal", "text": "."},
        {"type": "value", "source": "level2"},
        {"type": "literal", "text": "."},
        {"type": "value", "source": "current"}
      ]
    }
  }
}
```

### 6.3 片段类型

- `value`
  - 引用某一级的值
  - `source` 可为 `current`、`level1` 到 `level8`
- `literal`
  - 固定文本
  - 如 `.`
  - 如 `-`
  - 如 `/`

### 6.4 关键约束

- `chain_catalog` 不负责加 `第`、`章`、`（`、`）` 之类的外壳文本。
- `chain_catalog` 只负责值链本身。

## 7. 部件四：快捷预设表 `preset_catalog`

为了避免用户每次从零组合，常见规则应有预设。

### 7.1 示例

| preset id | UI 名称 | 含义 |
| --- | --- | --- |
| `chapter_cn` | `第一章` | 外壳 `第{}章` + 当前值样式 `一 二 三` + 仅当前 |
| `section_cn_arabic` | `第1节` | 外壳 `第{}节` + 当前值样式 `1 2 3` + 仅当前 |
| `ordinal_cn` | `一、` | 外壳 `{}、` + 当前值样式 `一 二 三` + 仅当前 |
| `paren_cn` | `（一）` | 外壳 `（{}）` + 当前值样式 `一 二 三` + 仅当前 |
| `decimal_2` | `1.1` | 外壳 `{}` + 当前值样式 `1 2 3` + 组合链 `一级.当前` |
| `decimal_3` | `1.1.1` | 外壳 `{}` + 当前值样式 `1 2 3` + 组合链 `一级.二级.当前` |

### 7.2 作用

- 简单模式直接选预设。
- 组合模式允许用户拆开改“外壳”和“内部数值样式”。
- 专家模式允许编辑组合链片段。

## 8. 部件五：层级绑定表 `level_bindings`

### 8.1 定义

每级标题不再直接存一整条模板字符串，而是绑定到上面的结构化部件。

### 8.2 建议结构

```json
{
  "level_bindings": {
    "heading1": {
      "enabled": true,
      "display_shell": "chapter_cn",
      "display_core_style": "cn_lower",
      "reference_core_style": "arabic",
      "chain": "current_only",
      "title_separator": "　",
      "start_at": 1,
      "restart_on": null,
      "include_in_toc": true
    },
    "heading2": {
      "enabled": true,
      "display_shell": "plain",
      "display_core_style": "arabic",
      "reference_core_style": "arabic",
      "chain": "l1_dot_current",
      "title_separator": "　",
      "start_at": 1,
      "restart_on": "heading1",
      "include_in_toc": true
    },
    "heading3": {
      "enabled": true,
      "display_shell": "paren_cn",
      "display_core_style": "cn_lower",
      "reference_core_style": "cn_lower",
      "chain": "current_only",
      "title_separator": "　",
      "start_at": 1,
      "restart_on": "heading2",
      "include_in_toc": true
    }
  }
}
```

### 8.3 字段说明

- `display_shell`
  - 当前级自己显示时使用的外壳
- `display_core_style`
  - 当前级自己显示时使用的内部数值样式
- `reference_core_style`
  - 当前级被下级组合链引用时使用的内部数值样式
- `chain`
  - 当前级的值链组合方式
- `title_separator`
  - 编号文本和标题正文之间的分隔符
- `start_at`
  - 起始编号
- `restart_on`
  - 遇到哪个上级时重置
- `include_in_toc`
  - 是否进入目录

## 9. 渲染规则

### 9.1 自身显示

当前级标题编号渲染流程：

1. 先按 `chain` 取出值链。
2. 值链中的 `current` 使用当前级的 `display_core_style`。
3. 值链中的上级引用默认使用对应层级的 `reference_core_style`。
4. 值链拼好后，再套当前级的 `display_shell`。

### 9.2 被下级引用

当某一级被下级引用时，不使用它自己的外壳，只使用它的 `reference_core_style`。

这就是为什么：

- 一级显示可以是 `第一章`
- 二级却仍然可以是 `1.1`

### 9.3 示例

配置：

- `heading1.display_shell = 第{}章`
- `heading1.display_core_style = 一 二 三`
- `heading1.reference_core_style = 1 2 3`
- `heading2.chain = 一级.当前`
- `heading2.display_shell = {}`
- `heading2.display_core_style = 1 2 3`

结果：

- 一级标题显示：`第一章`
- 二级标题显示：`1.1`

## 10. UI 设计

## 10.1 主表格列

用户提出的核心需求应直接体现在 UI 表格中。

建议列如下：

1. `预览`
   - 第一列
   - 直接显示当前行配置生成的最终标题效果
   - 选项示例：
     - `第一章　绪论`
     - `1.1　研究背景`
     - `（一）　实验条件`
   - 预览应至少包含：
     - 编号文本
     - 标题分隔符
     - 示例标题正文
2. `外壳`
   - 第二列
   - 选项示例：
     - `第{}章`
     - `第{}节`
     - `{}`
     - `{}、`
     - `（{}）`
     - `附录{}`
     - `自定义`
3. `内部样式`
   - 第三列
   - 选项示例：
     - `1 2 3`
     - `01 02 03`
     - `一 二 三`
     - `壹 贰 叁`
     - `I II III`
     - `A B C`
4. `多级组合`
   - 第四列
   - 选项示例：
     - `仅当前`
     - `一级.当前`
     - `一级.二级.当前`
     - `自定义`
5. `被下级引用时`
   - 第五列
   - 选项示例：
     - `沿用内部样式`
     - `1 2 3`
     - `01 02 03`
     - `一 二 三`
     - `自定义`
6. `标题分隔符`
7. `起始值`
8. `重置于`
9. `进入目录`
10. `启用`

### 10.2 为什么预览必须放在第一列

预览不是附属信息，而是主信息。

因为用户真正关心的不是：

- 当前 shell 是什么
- 当前 core style 是什么

而是：

- 最终标题是不是 `第一章　绪论`
- 最终标题是不是 `1.1　研究背景`

所以预览必须放在第一列，让用户先看最终效果，再调整后面的结构化部件。

### 10.3 为什么必须有第五列

只做前两列还不够。

因为下面这种需求无法表达：

- 一级显示：`第一章`
- 二级显示：`1.1`

这就要求：

- 一级当前显示时内部样式是 `一 二 三`
- 一级被下级引用时内部样式却是 `1 2 3`

所以“被下级引用时的样式”这一列仍然是必要的。

### 10.4 交互模式

建议分三种模式：

- 简单模式
  - 直接选预设，如 `第一章`、`1.1`、`（一）`
- 组合模式
  - 按列选择：外壳 + 内部样式 + 多级组合 + 引用样式
- 专家模式
  - 编辑 `chain_catalog.segments`

## 11. 推荐默认方案

| 层级 | 外壳 | 内部样式 | 多级组合 | 被下级引用时 |
| --- | --- | --- | --- | --- |
| 一级 | `第{}章` | `一 二 三` | `仅当前` | `1 2 3` |
| 二级 | `{}` | `1 2 3` | `一级.当前` | `1 2 3` |
| 三级 | `（{}）` | `一 二 三` | `仅当前` | `一 二 三` |
| 四级 | `{}` | `1 2 3` | `一级.二级.三级.当前` 或自定义 | `1 2 3` |

## 12. 校验规则

必须校验：

- `display_shell` 必须存在于 `shell_catalog`
- `display_core_style` 必须存在于 `core_style_catalog`
- `reference_core_style` 必须存在于 `core_style_catalog`
- `chain` 必须存在于 `chain_catalog`
- `start_at >= 1`
- `restart_on` 只能引用更高层级
- `chain.segments` 中的 `source` 只能是 `current` 或 `level1` 到 `level8`
- `shell` 只能有一个值槽

## 13. 与旧配置的迁移思路

旧模型：

- `format`
- `template`
- `separator`

迁移到新模型时，不再一对一直接平移，而是拆解。

### 13.1 典型映射

| 旧配置 | 新映射 |
| --- | --- |
| `chinese_chapter + 第{cn}章` | `display_shell=chapter_cn`, `display_core_style=cn_lower`, `chain=current_only` |
| `chinese_section + 第{cn}节` | `display_shell=section_cn`, `display_core_style=cn_lower`, `chain=current_only` |
| `chinese_ordinal + {cn}、` | `display_shell=dunhao_cn`, `display_core_style=cn_lower`, `chain=current_only` |
| `chinese_ordinal_paren + （{cn}）` | `display_shell=paren_cn`, `display_core_style=cn_lower`, `chain=current_only` |
| `arabic_dotted + {parent}.{n}` | `display_shell=plain`, `display_core_style=arabic`, `chain=按层级展开` |

### 13.2 旧 `{parent}.{n}` 的迁移

- `heading2` -> `一级.当前`
- `heading3` -> `一级.二级.当前`
- `heading4` -> `一级.二级.三级.当前`

## 14. 首版实现建议

建议按以下顺序实现：

1. 先引入新数据层：
   - `shell_catalog`
   - `core_style_catalog`
   - `chain_catalog`
   - `preset_catalog`
   - `level_bindings`
2. 先做简单模式与组合模式，不急着开放专家模式。
3. 先支持前 4 级标题。
4. 渲染链路统一使用结构化数据，不再以旧模板字符串为主。
5. 旧配置保留兼容读取，但新保存统一写入解构式结构。

## 15. 结论

最终模型不应该是：

- 每级一个 `format + template`

而应该是：

- 第一列看“最终预览”
- 第二列选“外壳”
- 第三列选“内部数值样式”
- 第四列选“多级组合”
- 第五列选“被下级引用时的样式”

再由层级绑定表把这些部件装配起来。

这才是适合多级编号、又能高度自定义、同时还容易做 UI 的架构。
